import React from 'react';
import UserInfo from './UserInfo'; // Import UserInfo component

function ProfilePage() {
  return <UserInfo />;  // Just render the UserInfo component without props
}

export default ProfilePage;
