import React from 'react';
import UserDetails from './UserDetails';  // Import UserDetails component

function UserInfo() {
  return <UserDetails />;  // Just render UserDetails without props
}

export default UserInfo;
